version: "2"

linters:
  default: standard
  disable:
    - depguard
  settings:
    errcheck:
      check-type-assertions: true
      check-blank: true
  exclusions:
    paths:
      - "_test\\.go$"
      - "internal/testing"
      - "internal/testutils"

run:
  timeout: 10m
