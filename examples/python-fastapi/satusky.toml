[app]
name = "python-fastapi"
port = 8000
cpu_request = "100m"
cpu_limit = "250m"
memory = "256Mi"

[checks]
health_path = "/health"

[checks.readiness]
initial_delay_seconds = 2
timeout_seconds = 2
period_seconds = 5

[checks.readiness.http_get]
path = "/health"
port = 8000
