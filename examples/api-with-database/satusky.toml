[app]
name = "guide-api-db-notes"
port = 8080
cpu_request = "50m"
cpu_limit = "200m"
memory = "256Mi"
replicas = 1

[build]
dockerfile = "Dockerfile"

[checks]
health_path = "/health"

[deploy]
strategy = "rolling"
rolling_max_surge = "1"
rolling_max_unavailable = "0"
