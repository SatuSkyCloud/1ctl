# API with PostgreSQL

This is the complete application used by the
[Deploy an API with PostgreSQL](https://docs.satusky.com/guides/api-with-database/)
guide.

Run the guide from this directory. Its `1ctl deploy --name "$APP" --wait`
command deliberately overrides the placeholder application name in
`satusky.toml`, so each test run can use an isolated name.
