[app]
name = "microservices-catalog"
port = 8001
cpu_request = "50m"
cpu_limit = "200m"
memory = "128Mi"

[checks]
health_path = "/health"

[checks.readiness]
initial_delay_seconds = 2
timeout_seconds = 2
period_seconds = 5

[checks.readiness.http_get]
path = "/health"
port = 8001
